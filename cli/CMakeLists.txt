# rummur-cli — Reference CLI tool (Phase 2)
# Stub: builds but contains no source yet.
# Phase 2 will add main.cpp and wire up send/receive/scan commands.
message(STATUS "rummur-cli: Phase 2 stub — no sources yet")
